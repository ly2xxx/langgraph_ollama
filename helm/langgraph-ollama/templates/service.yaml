apiVersion: v1
kind: Service
metadata:
  name: {{ include "langgraph-ollama.fullname" . }}
  labels:
    {{- include "langgraph-ollama.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "langgraph-ollama.selectorLabels" . | nindent 4 }}
