"""Coding Engineer — non-stop coding agent.

See CODING_ENGINEER.md in this directory for the full design. This package
is intentionally self-contained (imports nothing from the rest of the repo
except shared conventions like `telemetry`), so it can be dropped into
`app.py` as a fourth agent without touching existing modules.
"""
