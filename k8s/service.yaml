apiVersion: v1
kind: Service
metadata:
  name: langgraph-streamlit-svc
  namespace: ai-apps
  labels:
    app.kubernetes.io/name: langgraph-streamlit
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8501
      targetPort: 8501
      protocol: TCP
  selector:
    app.kubernetes.io/name: langgraph-streamlit
