apiVersion: v1
kind: Secret
metadata:
  name: langgraph-streamlit-secret
  namespace: ai-apps
type: Opaque
stringData:
  # Replace with your actual Tavily API key or inject via Rancher Secrets UI
  TAVILY_API_KEY: "your_tavily_api_key_here"
