"""FastAPI wrapper around the Coding Engineer agent.

Kept in its own package so the agent (coding_agent/) stays importable and
testable without FastAPI installed. Nothing here is imported by coding_agent.
"""
